[project]
name = "rain-predicition"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ANBU007-MCET", email = "kanbuselvan2007@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
rain-predicition = "rain_predicition:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
